FROM python:3.12-slim

WORKDIR /app
ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1

RUN pip install --no-cache-dir uv
COPY apps/agent ./
RUN uv sync --frozen --no-dev

CMD ["uv", "run", "--no-sync", "python", "-m", "agent.main", "start"]
