# dependencies
node_modules/
.pnpm-store/

# build output
dist/
.next/
out/
storybook-static/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example
# loose secrets dropped at the repo root (provider/API keys, never committed)
api-keys.txt

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.hypothesis/

# infra data volumes
infra/docker/.appwrite/
infra/docker/.livekit/
appwrite_data/
livekit_data/

# test / tooling
coverage/
playwright-report/
test-results/
.DS_Store

# figma make reference export (kept locally as a design reference, not committed)
_figma_make_export/
_bmad/
_bmad-output/
Recreate Front-End Page*.zip
Design Interviewer Page*.zip

# local scratch / one-off debug (kept locally, never committed)
scripts/_dbg.mjs
handoff.md

# installed agent tooling (never vendored into the repo; like _bmad/)
.agents/
.kiro/skills/
.opencode/
.vscode/
