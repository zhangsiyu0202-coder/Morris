# Decision Log — SPEC-qwen-vl-llm-swap

## 2026-06-25 — Initial creation (pass from pre-authored requirements/design/tasks)

**Pass 1 — Coherence.** Six capabilities cover the full change surface: LLM resolution (CAP-1), adapter construction (CAP-2), stimulus flow + per-type instructions (CAP-3), image injection (CAP-4), Gemini decoupling (CAP-5), and engine guards (CAP-6). Each has a concrete success criterion. Constraints rule out DeepSeek deletion, Gemini modification, contracts changes, and eager livekit imports. Non-goals explicitly defer video frame extraction and ADR-0011. Success signal is `pnpm test:py` green + mode-specific provider-settings assertions.

**Pass 2 — Preservation.** All eight requirements from `requirements.md` are mapped: R1→CAP-1, R2→CAP-2, R3+R4→CAP-3, R5→CAP-4, R6→CAP-5, R7→CAP-6, R8→met by the success signal. The design's data-flow diagram, module change table, and target code fragments live in `design.md` (companion). The task dependency graph and implementation steps live in `tasks.md` (companion). The §6 decision (Gemini speech decoupling, option A) is captured in CAP-5 and design.md. No load-bearing claim was dropped.

**Verdict:** SPEC.md kernel is lean, all load-bearing detail lives in companions. Ready for downstream implementation.
